#! api-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: express-backend
  labels:
    com.docker.compose.project: express-backend
    com.docker.compose.service: api
spec:
  replicas: 1
  selector:
    matchLabels:
      com.docker.compose.project: express-backend
      com.docker.compose.service: api
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        com.docker.compose.project: express-backend
        com.docker.compose.service: api
        com.docker.compose.network.default: "true"
    spec:
      containers:
        - name: api
          image: akshay2642005/express-backend-api:dev
          imagePullPolicy: IfNotPresent
          command: ["yarn", "dev"]
          env:
            - name: MONGO_URI
              value: "mongodb://mongodb:27017/express-rest-es2017-boilerplate"
            - name: NODE_ENV
              value: "production"
          ports:
            - name: api-3000
              containerPort: 3000
