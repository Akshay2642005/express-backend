#! mongodb-mongo_db-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: mongodb-mongo-db
    namespace: express-backend
    labels:
        com.docker.compose.service: mongodb
        com.docker.compose.volume: mongo_db
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
